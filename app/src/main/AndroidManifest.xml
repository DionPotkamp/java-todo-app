<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Todo"
        tools:targetApi="31">
        <activity
            android:name=".TodoDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_todo_detail"
            android:theme="@style/Theme.Todo.NoActionBar" />
        <activity
            android:name=".TodoCreateActivity"
            android:label="@string/title_activity_todo_create_update"
            android:noHistory="true"
            android:exported="false" />
        <activity
            android:name=".TodoUpdateActivity"
            android:label="@string/title_activity_todo_create_update"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Todo.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>